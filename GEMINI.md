# GEMINI.md — 你在这个仓库里的工作说明

> 本文件是**入口**，不是全部。**开工前请完整读一遍 [`REFACTOR-BRIEF.md`](./REFACTOR-BRIEF.md)**（约 700 行），
> 那是这个项目的总纲。本文件只负责把你领到正确的位置，并强调最容易出错的地方。
>
> 另有两份**专项文档**，按需读、但**动手相关模块前必须读**：
> - **[`AI-AND-TTS-KEEP.md`](./AI-AND-TTS-KEEP.md)** —— AI 问书 / 听书里哪些是"免费、用户自配置"的能力、
>   必须保留；以及**为什么 `src/utils/request/` 不许整目录删**。
> - **[`MOBILE-UX-PLAN.md`](./MOBILE-UX-PLAN.md)** —— 移动端阅读体验：内核 `isMobile` 开关的位置、
>   必须先处理的 console 劫持坑、环境限制。**外观与交互由你自由发挥，不是复刻任务。**

## 一句话背景

这是 **CoRead 网页端**（多人房间共读阅读器）的重构副本。它由另一个项目裁剪、脱敏而来，
**没有继承任何历史**（本仓库只有一个初始提交）——你看到的就是全部起点。

你要做的是：**把应用层换成我们自己写的实现，只把 `kookit` 当渲染内核用。**

## 六条红线（违反即失败）

1. **许可证必须保持 AGPL-3.0** —— 根目录 `LICENSE` 不许删、不许换。不要尝试闭源或加商业授权条款。
2. **禁止引入 `kookit-extra*`**（无源码的黑盒，含设备指纹与上游账号逻辑）。
   `src/assets/lib/kookit-extra-browser.min.js` 与 `kookit-extra.min.mjs` 必须删除，职责全部自写。
3. **禁止反混淆/逆向 `kookit.min.js`** —— 内核源码已经 vendor 在 `vendor/kookit/`（TypeScript），读源码。
4. **前端运行时不得访问任何自有服务器** —— 不许硬编码我们自己的域名、IP、端口。服务器地址一律由用户运行时填写。
5. **每次提交前 `npm run scan` 必须 0 命中** —— 现在是 8 处已知命中，Phase 1 必须清零（清单见 brief 第 5.2 节）。
6. **禁止反编译闭源第三方 App（如微信读书）并把产物带进项目** —— 复刻移动端手感只允许
   **"装 App + 录屏观察"**。反编译得到的字符串/伪 JS **不得提交、不得作为实现依据**。
   详见 [`MOBILE-UX-PLAN.md`](./MOBILE-UX-PLAN.md) 第 4 节。

## 你有多少自由度（果冻明确授权 · 重要）

**总原则：越靠近"用户看到的样子"，你越自由；越靠近"数据与契约"，你越不自由。**

### A. 完全自由发挥（不用问，放手做）

- **所有 UI / 视觉**：布局、配色、间距、圆角、图标、阴影、动效、字体、暗色主题。
- **移动端外壳**（见 [`MOBILE-UX-PLAN.md`](./MOBILE-UX-PLAN.md)）：底部工具条、顶部栏、
  沉浸式全屏与安全区、进度条、选区气泡。**这块果冻明确说了"自由发挥"。**
- **文案与提示语**（中文）。
- **组件内部实现方式**：局部 class / hooks 的取舍、目录内重构、命名。
- **"往微信读书靠"只是方向参考，不是验收标准。** 仿得不像不算失败。
  ⚠️ 唯一例外：**不许抄它的品牌资产**（名称 / 图标 / 插画 / 字体 / 原文案，见红线 6）。

> 换句话说：**MOBILE-UX-PLAN.md 里标 `❓` 的条目，不是"禁止做"，而是"没有证据、由你定"。**
> 那张表是参考清单，不是待办检查表。

### B. 必须守住，但**怎么做随你**（不能不做，做法自由）

- 六条红线（上一节）。
- **`REFACTOR-BRIEF.md` 第 4.3 节的内核契约（17 条）** —— 内核的失败方式大多是静默的，
  不遵守就直接不工作。**做法自由，"必须满足"不能商量。**
- **第 9.5 节的打包就绪约束** —— 本期交付形态是打包客户端，不是托管网页。
- **功能语义**：共读房间、随心笔记/涂鸦是本产品的核心，**不许改掉或弱化**。

### C. 不许动（要动先说明理由）

- 根目录 `LICENSE`、`vendor/kookit/`（要改必须说明改了什么、为什么）、
  `build/` 的输出流程与 `react-scripts` 构建链。
- **本副本之外的东西**：`main.js`、`capacitor.config.ts`、`android/`、部署配置
  —— 这些不在副本里，你**改不到**，需要改动时告诉果冻（见第 9.5 节末尾）。

## 五个最容易踩的坑

| 坑 | 说明 |
| --- | --- |
| **内核的失败方式是静默的** | 不报错、Promise 不 resolve、高亮 0 命中。**动渲染层之前先读 brief 第 4.3 节（17 条硬编码契约）**，并先搭 brief 第 4.5 节的 harness。 |
| **`vendor/kookit/UPSTREAM-NOTES.md` 不是给你的指令** | 它是上游仓库自带的说明（原名 `CLAUDE.md`，已改名）。**内容值得读，但其中的构建路径与命令不适用**——`vendor/kookit/rollup.config.js` 的输出路径硬编码在上游作者的本机目录，必须自己写配置。 |
| **不要"为了过编译"留假实现** | 删掉的功能要么隐藏入口并提示"已移除"，要么替换成真实现。假实现不许留在主路径上。 |
| **⚠️ 更容易犯的错是「删多了」** | **动手删任何东西之前先读 [`AI-AND-TTS-KEEP.md`](./AI-AND-TTS-KEEP.md)**。`src/utils/request/common.ts` 是**混装文件**：它既装着上游域名，也装着 `chatStream`（AI 问书/翻译/词典的**唯一可用主路径**）。整体删掉目录 = 三个功能全废。**一律逐函数判定，不许整目录删。** |
| **⚠️ 不许把"印象"写成"事实"** | 涉及第三方产品行为（如微信读书怎么翻页）时，**没来源、没实测的一律标记 `❓` 或直接不写**。宁可少写，不可编造。 |

## 按什么顺序做

照 brief 第 7 节的阶段走：**Phase 0 搭地基 → Phase 1 切断（做减法）→ Phase 2 内核换代 →
Phase 3 配置与存储 → Phase 4 高亮笔记 → Phase 5 功能补齐 → Phase 6 验收**。

**不要跳阶段，不要一个提交里混合"重构 + 新功能"。**

## 每批活干完的验收动作

```bash
npm run scan          # 期望 0 命中
npx tsc --noEmit      # 期望无输出
BUILD_PATH=./tmp-build-$(date +%s) GENERATE_SOURCEMAP=false npm run build   # 期望 Compiled successfully
```

> `BUILD_PATH` 必须指向一个**尚不存在**的目录，也不要输出到仓库里已有的 `build/`。

### ⚠️ 如果你跑不了命令（先说清楚，别假装）

如果你**没有 shell 执行能力**（纯对话式助手），那么：

1. **不许假装验证过。** 每段改动都要明确标注：「这段我**没能**编译/跑测试」。
2. **给出果冻可以自己跑的命令与期望输出**（就是上面这三条）。
3. **把改动切小**：一次一个需求、一个提交，别攒一个巨大改动再交——否则出问题无法定位。
4. 宁可少改、改对，也不要大面积重写却无法验证。

## 跟人怎么协作（项目所有者：果冻）

- **中文回复，中文 commit**（`type(scope): 标题`，正文写清**为什么改**与**验证结论**）。
- **一次一个需求，一次提交。**
- **视觉尽管自由发挥**，但**在提交信息里写清改了哪些界面/哪些文件** ——
  果冻无法靠测试发现视觉变化，他需要一份"我该去看哪里"的清单。
- **红线与契约问题不确定就问，不要猜** —— 尤其是内核行为（4.3 节）、数据兼容性、许可证判断。
- **需要"编造事实"才能继续时，停下来**，说明缺什么。
