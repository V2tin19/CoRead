# 移动端阅读体验：往「微信读书」那种手感靠，怎么做

> 配套文档：[`REFACTOR-BRIEF.md`](./REFACTOR-BRIEF.md)（总纲）、[`AI-AND-TTS-KEEP.md`](./AI-AND-TTS-KEEP.md)。
> 本文档给方向、给坑、给动手顺序，**不含代码**。

> 🔓 **定位声明（果冻 2026-09-15 授权）**：本文档是**方向参考，不是验收标准**。
> **移动端外观与交互由实现者自由发挥**，不需要逐条请示、不需要"人工确认"。
> 文档里标 `❓` 的条目**不是"禁止做"，而是"没有公开证据、由你定"**——
> 那张表是**参考清单**，不是待办检查表。仿得不像微信读书，**不算失败**。
> ⚠️ 唯一红线：**不许抄它的品牌资产**（名称/图标/插画/字体/原文案）。

> **勘误（2026-09-14，果冻指出）**：本文档早期版本写过「微信读书默认上下滚动为主」，
> **这是错的**。经 4 个独立来源核实，微信读书**默认是左右滑动翻页**，可在设置改为
> 仿真翻页 / 上下滚动 / 覆盖翻页 / 无动画。已全文修正。
> 同时新增纪律：**凡无来源或无实测支撑的描述，一律加 `❓` 标记。**

---

## 0. 范围先说清：本副本里做的是「Web 层的移动端体验」，**不是打包**

⚠️ 容易被误解，先划清：

| 事项 | 在本副本里？ |
| --- | --- |
| 移动端**页面与交互**（响应式布局、`isMobile` 开关、手势、底部工具条、沉浸式 CSS） | ✅ **是，这是本文档要你做的** |
| Capacitor 安卓外壳、`capacitor.config.ts`、`android/`、签名与出 APK | ❌ **不在**（见 brief 附录 C） |
| 桌面 Electron 外壳、`main.js` | ❌ **不在** |

⇒ 你要交付的是**"Web 页面在手机尺寸下好用"**，打包方（果冻）会把这套 Web 产物包进 APK。
所以：**不许引入原生模块依赖**（brief 第 9.5 节第 3 条），`npm run build` 的产物必须能在
`https://localhost`（Capacitor）和 `file://`（Electron）下都跑得起来。

**什么时候做**：移动端是**并行轨道**，不占重构的 Phase 0–2。
建议顺序见第 3 节（先打通 `isMobile`，再动外观）。

---

## 1. 关键前提：内核**已经有移动端模式**，只是我们把它关着

`kookit` 的 `GeneralRender` 接收一个 `isMobile` 配置。我们的代码里有 **6 处传参，
全部写死 `"no"`**，其中**真正开书的那一处**是：

```
src/containers/viewer/component.tsx:326     isMobile: "no",      ← 实际渲染走这里
src/containers/cloudLibrary/component.tsx:319
src/components/importLocal/component.tsx:298
src/components/roomImportButton/component.tsx:51
src/components/dialogs/moreAction/component.tsx:291
src/utils/common.ts:762                     （preCache 缓存路径，与显示无关，保持 "no" 是对的）
```

`src/` 里**没有任何地方读取** `isMobile` —— 它只被内核消费。所以：
**打开移动端手感的第一颗开关，就是让开书路径按运行形态传 `"yes"`。**

### `isMobile: "yes"` 在内核里到底改了什么（本次实测，共 5 处）

| # | 行为 | `"no"`（现状） | `"yes"` |
| --- | --- | --- | --- |
| 1 | 翻页滚动 | `behavior: "smooth"`（平滑滚动） | `behavior: "auto"`（**瞬时跳转**） |
| 2 | 翻页前等待 | 动画非 `none` 时 `sleep(320ms)` | **跳过这 320ms** |
| 3 | 是否允许 iframe 内脚本 | 按配置 `isAllowScript` | **强制 `"yes"`** |
| 4 | 触摸事件 | 仍会注册（`addTouchEvent` 被调用） | 同左，但这是"安卓/苹果两套实现"的入口（第一个参数其实是**是否安卓**） |
| 5 | `console.log/info/error` | 原样 | ⚠️ **被改写为 `window.ReactNativeWebView.postMessage(...)`** |

> ⚠️ **第 5 条是个坑，必须先处理**：那段劫持是给 Koodo 自己的 **React Native** 客户端用的，
> 而我们是 **Capacitor**（没有 `ReactNativeWebView` 这个桥）。
> 直接传 `"yes"` ⇒ 之后**每一次 `console.log` 都会抛错**，而且报错信息本身还要走 console……
> 处置：① 在页面里放一个 `window.ReactNativeWebView = { postMessage(){} }` 的兼容桩；或
> ② 改 vendor 源码，把这段劫持加上环境守卫（**推荐**，属于我们要维护内核的理由之一）。

---

## 2. 「微信读书那种体验」拆成 5 块，逐块对我们现状

> ⚠️ 下表每一行都标了**证据等级**：`✅已证实` = 有公开来源或本机实测；
> `❓未证实` = 没有公开证据。**`❓` 不等于禁止做，等于"由实现者自己定"。**
> 本表的作用是**让你知道哪些是查过的、哪些是自己的判断**，而不是限制你。

| 块 | 微信读书的做法（附证据） | 我们的现状 | 代价 |
| --- | --- | --- | --- |
| **A. 沉浸式** | 全屏阅读、隐状态栏，正文避开刘海/圆角 | 无全屏控制；**全项目没有任何 `safe-area-inset` 样式** | 中。安卓要接 Capacitor 状态栏/全屏能力（新增原生插件）+ 一套 safe-area 内边距。**自由发挥** |
| **B. 翻页与手势** | ✅**默认「左右滑动」翻页**（左右滑 = 上一页/下一页，像翻纸书）；设置里可改 **仿真翻页 / 上下滚动 / 覆盖翻页 / 无动画**（"微信读书-设置-翻页方式"，4 个独立来源一致，2026-09 核实）<br>❓点按左/右=翻页、点按中间=呼出菜单（常见说法，未见权威来源，**由你定**） | 内核有触摸规则与两套实现；`scroll` 模式滚动发生在**宿主容器**上；动画默认跟随用户设置、触屏兜底 `"sliding"`（`viewer/component.tsx:300-304`） | 中。翻页四模式内核基本都能对上；点按分区若内核不给，可在自己外层加一层手势识别 |
| **C. 呼出菜单** | ❓顶部栏 + 底部**单排**工具条（目录/进度/听书/字号/背景/更多） | 有 `header` 与 `readerSettings`，但是**桌面式面板**（侧边/弹窗、项多） | 中偏大。要做移动端外壳：底部 sheet、顶部细栏、点击空白收起。**渲染层一行不用动。这块自由发挥** |
| **D. 进度** | ❓底部进度条可拖拽 + 章节名 + 百分比 | 有进度与章节能力（内核提供 `getPosition`/`goToChapter`） | 小。主要是 UI 与拖拽交互（注意 `record()` 有 1000ms 动画等待，见总纲 4.3 第 11 条） |
| **E. 划线/选词** | ❓长按选词 → 气泡菜单（划线/想法/复制/查词/翻译） | **已经有 `popupBox` 一套**（`popupNote` 划线 + `popupTrans` 翻译 + `popupDict` 查词 + `popupAssist` 问书） | 小。把弹层改成"贴在选区旁的气泡"即可，能力是现成的 |

> 结论：**A/C 是真正的工作量，B/D/E 是"接线 + 美化"。**
> 渲染内核不用换，这是我们坚持 kookit 的回报。
> **C/D/E 没有公开证据，所以它们的"具体长相"由实现者定**——见第 4 节。

---

## 3. 动手顺序（建议，别跳）

1. **直接动手，别卡在"取证"上。** 移动端外观和交互**由你自由发挥**——
   不必先做交互规格表、不必等果冻给参考。（如果果冻日后给了录屏，照录屏微调即可。）
   → 唯一要守的护栏是**不抄品牌资产**（名称/图标/插画/字体/原文案），
     以及第 1 节那个 `isMobile` + console 劫持的前置处理。
2. **打通 `isMobile` 开关 + 处理 console 劫持**（第 1 节）。⚠️ 这一步之前不要动视觉。
   有真机就在真机上看，没有就先在 Chrome DevTools 手机模拟里迭代。
3. **做移动端外壳（A/C）**：全屏与安全区、底部工具条、顶部栏、呼出/收起动画。
4. **接 D/E**：进度条拖拽、划线气泡。E 基本是复用现有 popup 组件。
5. **真机验收**（本机有环境时）：触摸手势、滚动惯性、返回到桌面/锁屏再回来、刘海遮挡、
   深色主题下的高亮可读性。**没有真机就如实说明"未真机验证"，不要假装测过。**

---

## 4. 「微信读书的依据」到底能不能拿到（**结论：不拿，长相自由发挥**）

> **你只需要记住这一节的结论**：我们**不提供**微信读书的实现依据，
> 也**不要求你复刻**它。移动端长相与交互**由你设计**。
>
> 下面留着是为了回答两个常见疑问：「为什么不直接搞到它的源码/反编译？」「我都查过了吗？」
> 想省时间的话，读完这段结论就可以跳过本节。

### 4.1 三条取证路，分别能拿到什么

| 路 | 能做到什么 | 能不能当实现依据 | 风险 |
| --- | --- | --- | --- |
| **① 源码** | **拿不到。** 微信读书是闭源商业软件，没有任何公开仓库 | ❌ | — |
| **② APK 反编译** | 能拿到**字符串**（API 端点、Prompt、密钥、错误文案）和**伪 JS 逻辑**（见 4.2） | ⚠️ 只能当"它有这个功能"的旁证，**不能当"它怎么实现"的依据** | ⚠️ **法律与合规风险**（见 4.4） |
| **③ 观察法** | 装 App 逐条操作 + **录屏**，逐帧量交互 | ✅ 唯一能拿到"手感级"精度的合法方式（**但不强制，做了也没必要**） | 无 |

> **一句话**：**不是"拿不到就只能瞎猜"，而是"我们要的本来就不是它的实现"**。
> 你要的不是它的代码，是**用户体验上一个好用的移动阅读器该有的样子** —— 那是设计的活，不是逆向的活。

### 4.2 如果真去反编译 APK，能到什么程度（已核实）

微信读书 Android 端是 **React Native**（v4.6.0，armeabi-v7a，kotlin + androidx —— 2026-09 经多来源核实）。
这决定了反编译的路径和天花板：

- **业务代码在哪**：`assets/index.android.bundle`，是 **Hermes 字节码**（RN 0.70+ 默认引擎），
  **不是明文 JS，也不是普通 dex**。传统 `jadx` 反编译 Java 那一套对它基本无效。
- **要什么工具**：`hbctool`、`hermes-dec`、`hasmer`、`hermes_rs` —— 能把字节码
  **反汇编成 HASM** 或 **反编译成"伪 JavaScript"**。
- **能拿到什么**：
  - ✅ **字符串**：用 `strings` 就能捞出 API 端点、请求头、错误提示、部分内置 Prompt/文案 —— 这些是**可读的**。
  - ✅ **功能存在的证据**：某个模块名/字符串出现，说明它确实有这个功能。
  - ✅ **可 patch**：改 HASM 再重汇编，理论上能重打包（**但这是明确越线的事，我们不做**）。
- **拿不到什么**（关键）：
  - ❌ **可读的控制流**。反编译出来是**寄存器式伪代码**（`r0/r1/r2` 到处传）、
    标签跳转、非结构化 —— **读不出"这个手势先判什么再判什么"**。
  - ❌ **精确的交互实现**。动画曲线、手势阈值、分区比例这些**全在实现里，不在字符串里**。
  - ❌ **美术资源与布局**。图片、字体、间距、圆角，反编译给不了可直接用的东西
    （而且**拿到了也不能用**，见 4.4）。
  - ❌ 混淆过的部分逻辑，读起来成本极高、收益极低。

> **结论**：反编译是"**能确认它有什么功能**"，不是"**能知道它怎么做的**"。
> 对我们"复刻手感"这个目标，**投入产出比极低**，且有法律风险。**建议不做。**

### 4.3 真正该参考的实现（都有合法许可证）

**学的应该是"交互模式"，不是"实现"。**

| 参考 | 许可证（已核实） | 值得看什么 |
| --- | --- | --- |
| **Legado（开源阅读）** | **GPL-3.0**（Kotlin，安卓，32k★） | **交互最接近微信读书的成熟开源实现**：翻页模式（覆盖/仿真/滑动/滚动）、手势分区、阅读设置浮层、TTS 与系统语音集成。看它的**交互组织方式**。 |
| foliate-js | MIT | 纯 Web 的阅读器 UI + 手势实现，**技术栈与我们最接近**，可读性最好 |
| epub.js 官方示例 | BSD-2 | 分页/滚动两种模式的手势与 life cycle 参考 |

> 注：Legado 的**翻页模式枚举**本身就印证了微信读书的四模式（仿真/覆盖/滑动/滚动），
> 这是"不用碰微信读书也能拿到参照"的最好例子。

### 4.4 法律边界（重要，务必遵守）

- 我们项目是 **AGPL-3.0**（因为用了 kookit）。**GPL-3.0 的代码可以并入 AGPL-3.0 项目**；
  MIT/BSD 更宽松。⇒ 参考、甚至移植片段在许可层面是可行的。
- **品牌与美术不能碰**：微信读书的名称、图标、插画、字体、文案一律不许出现；
  界面的"配色/圆角/图标"要做出我们自己的样子。**学的是信息架构与手势语义**。
- **反编译出的东西一律不进代码库**：即便反编译得到字符串/伪代码，
  把它搬进我们的项目 = 拿人家的私有实现，**法律上站不住，且会污染我们的 AGPL 声明**。
  ⇒ 反编译的产物**只允许放在自己电脑上当"功能存在性"的笔记，严禁提交**。
- 任何引入/移植都要**在提交信息里写明来源与许可证**（见总纲 8.5 的第三方声明要求）。

---

## 5. 环境限制：真机验证可能做不到（**不是阻塞，是"如实说明"**）

> ⚠️ 前提：以下是**果冻本机**的情况。如果你（实现者）跑在别的环境，以你实际环境为准。

果冻本机目前不具备安卓构建条件：

| 项 | 现状 | 需求 |
| --- | --- | --- |
| Android SDK / Android Studio | **未安装** | Capacitor 构建必需 |
| JDK | **25** | Capacitor 8 推荐 **17~21**（25 大概率不兼容 Gradle 插件） |

⇒ 结论不是"不能做"，而是**分级**：

- **移动端代码照常写**，Web 层的能力（CSS 媒体查询、`isMobile` 开关、手势接线）**不依赖安卓环境**。
- 迭代主要在 **Chrome DevTools 手机模拟**里做，这够用。
- **真机才有的结论**（触摸惯性、沉浸式遮挡、锁屏返回、软键盘）——**没测就如实说没测**，
  不要写"已验证"。这一段告诉果冻即可，**不需要他先拍板**。
- 若果冻后续补了环境，再补齐真机验收。

---

## 6. 已知会咬人的五个坑（写代码前先记住）

1. **`scroll` 模式下滚动发生在宿主容器上**（内核把 iframe 拉高到 `body.scrollHeight + 300`），
   宿主容器必须 `overflow-y: auto`，且 **CSS 不许给里面 iframe 设 `height: 100%`**
   ——否则 iframe 被压回一屏、容器不可滚，翻页退化成跳章（总纲 4.3 第 8 条）。
2. **PDF 的 `scroll` 模式不拉高外层 iframe**，要自己补 `scrollHeight + 300`（第 9 条）。
3. **文字类渲染不监听宿主容器 scroll**，`next()` 一开始就 `record()` ⇒ 位置是旧值，
   必须在滚动停稳后补一次 `record()`（第 10 条）。
4. **`record()` 在非 `none` 动画下会 sleep 1000ms** ⇒ 别把它挂在滚动回调里高频调用（第 11 条）。
5. **返回键/侧滑返回**要和"呼出菜单"抢事件。安卓端要显式定义：
   菜单开着时返回 = 收起菜单；否则 = 退出阅读。**这条只能真机调。**

---

## 7. 翻页默认值对照，以及几个"由你定"的点

- **翻页模式默认值 —— 好消息：我们和微信读书、方向本来就是对的。**
  - 微信读书**默认「左右滑动」**，另有 仿真翻页 / 上下滚动 / 覆盖翻页 / 无动画。
  - 我们的翻页"动画"选项是 `none`(None) / `sliding`(Slide) / `mimical`(Flip)，
    见 `src/constants/dropdownList.tsx:139-148`；"滚动 vs 分页"是另一个开关
    `readerMode === "scroll"`（`viewer/component.tsx:207-208`）。
  - 触屏设备我们的兜底正好是 **`"sliding"`（左右滑动）**（`viewer/component.tsx:300-304`）
    ⇒ **和微信读书默认一致，不用改。**
  - 对照表：

    | 微信读书 | 我们 |
    | --- | --- |
    | 左右滑动（默认） | `animation: "sliding"`（触屏兜底已是） |
    | 仿真翻页 | `animation: "mimical"` |
    | 无动画 | `animation: "none"` |
    | 上下滚动 | `readerMode: "scroll"` |
    | 覆盖翻页 | 内核没有直接对应（要不要自己做，由你定） |

- **沉浸式的程度**：彻底全屏（连状态栏都隐）还是保留状态栏？**由你定。**
- **要不要在移动端设置里把"上下滚动"做成显式可选项**（内核支持，接线即可）：**由你定。**
- **要不要为"取证"投成本**（见第 4 节）：**不用投。** 移动端长相自由发挥，
  不搞反编译、也不强求果冻录屏。

> ⚠️ **唯一一个不是"自由发挥"、而是"需要知道"的点**：`isMobile: "yes"` 会让内核
> **强制放行 iframe 内脚本**（第 1 节第 3 条）。这是内核既定行为，便利 vs 安全由产品承受。
> 你若判断需要收紧，就在代码注释里写明理由，并告诉果冻。
