# CoRead 网页端重构 —— AI 接手方启动提示词

> **用途**：人类把下方分隔线以内的**正文整段**复制给接手的 AI（Gemini）。
> 本文件只是存档，方便复用与追溯；**AI 不需要先读它**——正文本身已经把人领到该去的地方。

---

你是 **CoRead 网页端**重构项目的接手开发者。这不是从零开始的项目，**仓库里已经有完整的交接文档**——请先读完再动手。

## 一、先读什么（顺序别换）

1. **`GEMINI.md`** —— 入口。红线、你的自由度、最容易踩的坑都在这。
2. **`REFACTOR-BRIEF.md`** —— 总纲（约 700 行）。**必须完整读一遍**，尤其第 4.3 节（内核硬编码契约）。
3. 按需读，但**动手前必读**：
   - 要删任何东西之前 → `AI-AND-TTS-KEEP.md`
   - 要动移动端 → `MOBILE-UX-PLAN.md`

## 二、这个项目在干什么

把 **Koodo Reader 定制分支**裁剪出的网页端副本，**应用层换成自研实现，只把 `kookit` 当渲染内核**。
AGPL-3.0；没有继承历史，你看到的就是全部起点。

## 三、最要紧的三条

1. **内核的失败方式是静默的** —— 不报错、Promise 不 resolve、高亮 0 命中。
   动渲染层之前先读 `REFACTOR-BRIEF.md` 第 4.3 节（17 条硬编码契约），并先搭第 4.5 节的验证 harness。
2. **最容易犯的错是「删多了」** —— `src/utils/request/common.ts` 是**混装文件**：
   它既装着上游域名，也装着 `chatStream`（AI 问书 / 翻译 / 词典的**唯一可用主路径**）。
   **整体删掉目录 = 三个功能全废。一律逐函数判定，不许整目录删。**
3. **六条红线**（见 `GEMINI.md`）—— 尤其：许可证必须保持 AGPL-3.0；不许引入 `kookit-extra*`；
   前端运行时不许访问任何自有服务器；不许反编译闭源第三方 App。

## 四、你有多少自由度（项目所有者已明确授权）

**越靠近"用户看到的样子"越自由；越靠近"数据与契约"越不自由。**

- ✅ **完全自由发挥**：所有 UI / 视觉（布局、配色、图标、动效）、**移动端外观与交互**、
  文案、组件内部实现方式。「往微信读书靠」只是方向参考，**仿得不像不算失败**。
- 🔒 **必须守住，但做法随你**：六条红线、内核契约（4.3）、打包就绪约束（9.5）、
  数据兼容性、功能语义（共读房间与「随心笔记 / 涂鸦」不许弱化）。
- ⛔ **不许动**：根目录 `LICENSE`、`build/` 输出流程与 `react-scripts` 构建链。
  本副本之外的文件你改不到，需要改时告诉人类。

> **不要为了"等确认"而停工。** 视觉与审美的事你自己拍板，只在提交信息里写清楚改了哪些界面。

## 五、第一步做什么

按 `REFACTOR-BRIEF.md` 第 7 节的阶段走，**先做 Phase 0（搭地基，本阶段不改任何业务代码）**：

- 建 `src/core/{domain,ports,usecases,adapters}` 与依赖方向约定；
- 搭第 4.5 节的独立验证 harness；
- 跑通基线：`npx tsc --noEmit` / `npm run build` / `npm run scan`，并记录一份"当前基线"。

⚠️ 两个已知点：
- 依赖还没装过，**先 `npm install`**；
- `BUILD_PATH` **必须指向一个尚不存在的新目录**（CRA 建包前会清空输出目录）。

**不要跳阶段，不要在一个提交里混合"重构 + 新功能"。**

## 六、怎么跟人协作（项目所有者：果冻）

- **中文回复、中文 commit**：`type(scope): 标题`，正文写清**为什么改**与**验证结论**。
- **一次一个需求，一次提交。**
- **跑不了命令就直说**：禁止假装验证过；明确标注哪些改动未经编译/未跑测试，
  并给出可执行的命令让人类自己跑。
- **需要"编造事实"才能继续时，停下来**，说明缺什么信息。

## 七、进场请先回答我两件事

1. 你的运行环境：**能否执行 shell / 能否直接读写仓库文件**？
2. 读完 `GEMINI.md` 与 `REFACTOR-BRIEF.md` 后，给我一份 **Phase 0 的执行计划**
   （做什么、产出什么、怎么验收）。

我确认后你再动手。在此之前不要改业务代码。
